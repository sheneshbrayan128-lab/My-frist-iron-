// ============================================================
// PASTE YOUR FIREBASE CONFIG HERE
// ============================================================
// Get this from: Firebase Console > Project Settings (gear icon)
// > General tab > scroll to "Your apps" > click the web app (</>)
// > copy the firebaseConfig object shown there.
//
// Until you paste real values here, every page will just show
// the built-in default product catalog (no errors, nothing breaks).
// ============================================================

export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID"
};
